//基本语法
/*
if(条件,成立执行的语句,不成立执行的语句);

*/
if (1==1,
	print('条件成立');
,
	print('条件不成立');//条件为真，不会执行
);//记得一定要有分号结束语句。

if (2!=2,
	print('2不等于2');//不会执行
,
if (3==3,
	print('2等于2 且3等于3');//会执行这句。
,
	print('3不等于3');//不会执行
);
);
print(crlf);
if(5==5,print('5=5成立'));//仅一个参数


//if可缩写为i