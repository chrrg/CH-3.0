//自定义函数

function(abc,
	now();
);//定义一个全局函数
//函数定义有先后顺序先运行定义后可调用函数

line('现在时间:'.abc);//执行abc会调用abc函数并返回值
///////////////////////////////////////////////////////////////////
//带一个参数的自定义函数
function(gethour,$time,hour($time));

line('第二个：'.gethour('22:23:45'));//通过时间获取小时

///////////////////////////////////////////////////////////////
//带多参数的自定义函数
function(ceshi,$a,$b,$c,
$c=$a+$b;
$c*5;//这里可以是代码或代码块
);

line('第三个：'.ceshi(12,45,78));

